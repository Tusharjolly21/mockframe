# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*

# builds / caches
.next/
out/
build/
dist/
.turbo/
.vercel/
*.tsbuildinfo
next-env.d.ts

# env & secrets — NEVER commit these
.env
.env.*
!.env.example
*.local
serviceAccount*.json
*-credentials.json
*.pem
*.key

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# test
coverage/

# Photoshop source — huge (100s of MB), NOT runtime assets. The app ships the
# extracted PNG/JPG plates under public/; the raw .psd/.psb live outside git.
psd/
*.psd
*.psb
.vercel
.env*
.superpowers/
