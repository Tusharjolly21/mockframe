# MockFrame Code Screenshots for VS Code

Select code, open the editor context menu, and choose **MockFrame: Create
Screenshot from Selection**. The extension opens MockFrame's code template with
the selected text, language and filename prefilled.

Run `npm install && npm run build` in this directory before packaging with
`npm run package`.
